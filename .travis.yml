language: "go"

os: linux
dist: bionic

jobs:
  include:

    - os: windows

    - language: go

      go: "1.13.x"
      env:
        global:
          GO111MODULE=off

      before_install:
        - echo Starting Building and Testing The Repository

      script:
        - go build -v ./...
        - go test -v ./...
